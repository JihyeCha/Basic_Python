score = int(input("점수를 입력하시오: "))   # 변수 score을 선언하고, 사용자로부터 점수를 정수형으로 받음
if score >= 90:   # 만약 점수가 90점보다 크거나 같다면,
    print("A 입니다.")   # 이를 출력하라.
elif score >= 80:   # 만약 점수가 80점보다 크거나 같다면,
    print("B 입니다.")   # 이를 출력하라.
elif score >= 70:   # 만약 점수가 70점보다 크거나 같다면,
    print("C 입니다.")   # 이를 출력하라.
elif score >= 60:   # 만약 점수가 60점보다 크거나 같다면,
    print("D 입니다.")   #이를 출력하라.
else :   # 이 외의 점수일 경우, 
    print("F 입니다.")   # 이를 출력하라.
